# yaml-language-server: $schema=https://ai-x-labs.github.io/TimeNet/schemas/dataset-card-v1.schema.json
# Dataset Card for the chengsenwang/tsqa connector: the single source for this dataset's metadata.
# The connector loads it via DatasetMetadata.from_yaml(); the curator compiles it plus the schema
# derived from the data into manifest.json.
yaml_schema_version: 1
dataset_id: chengsenwang/tsqa
dataset_version: 1.0.0
name: TSQA
description: "Time-series question answering: a series plus a question/answer per sample."
license: Apache-2.0
domains:
  - general
tags:
  - qa
  - time-series
  - huggingface
